{
  "name": "smart-planner-dist",
  "version": "2.0.1",
  "description": "Build Kit for Smart Planner",
  "scripts": {
    "sync": "npx cap sync",
    "open": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@codetrix-studio/capacitor-google-auth": "^3.3.4",
    "@capacitor/ios": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
